meta {
  name: Unauthorized access
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/users/me
  body: none
  auth: none
}

tests {
  test("returns 401 without auth", function() {
    expect(res.status).to.equal(401);
  });
}
