// Electron main process entry point.
// TODO: Implement BrowserWindow creation, IPC handlers, tray, auto-update.
//
// The renderer process uses the same @librecord/ui-web components
// with @librecord/platform-electron (which re-exports platform-web).
